Nome: Gabriel Macedo da Silva
RA: 160949
Disciplina: Inteligência Artificial
Docente: Hermeson Pistori

----------------------------------
ANALISADOR

#Resumo

>O programa utiliza entradas como [] e [] para gerar uma saída que contém []

>É utilizado uma função que cria um mapeamento entre valores nítidos e difusos através de uma função de pertinência padrão

>

-----------------------------------
INSTALAÇÃO DAS BIBLIOTECAS

# Após abrir o terminal, utilize os comandos abaixo para instalar as bibliotecas necessárias:

> sudo apt install python3-pip
> pip3 install --user networkx==2.3
> pip3 install --user scikit-fuzzy
> pip3 install --user matplotlib
> pip3 install --user numpy

------------------------------------
UTILIZAÇÃO

Após realizar o download do arquivo siga os seguintes passos para executá-lo:

>Extraia o arquivo
>Com o terminal aberto vá até a pasta que foi extraida
>Escreva o comando "python Logica_Fuzzy.py"
>Será aberto uma interface que representa a aplicação
>Insira um valor de 0 à 10 em cada campo
>Clique em calcular
>O resultado será exibido na parte inferior da tela

--------------------------------------
